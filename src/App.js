import React from 'react';
import ShoppingContainer from './containers/ShoppingContainer';
import './App.css';

const App = (props) => {
  return (
    <ShoppingContainer />

  );
};

export default App
